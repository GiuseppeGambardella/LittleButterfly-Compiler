@* Test flussi di controllo (If/While) *@
fly() {
    ifint x = 5;
    whifilefe x > 0 thefen {
        ifif x == 3 thefen {
            flyout("Tre");
        } efelsefe {
            flyout(x);
        }
        x = x - 1;
    }
}